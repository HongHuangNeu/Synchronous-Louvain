package tt

object Setting {
  val oneLevel = false // true if you only want the result for one level
  val oneIteration = false //true if you only want one iteration
  val graphHome = "/home/honghuang/Documents/benchmark/1000_1"
  val graphFileName = "network.dat"
  val numOfNodes = 1000L
}